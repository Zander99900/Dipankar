names = "Zander, Dipankar"
egname = "Jantu"
len1 = len(names)
print(len1)
print(names[0:6]) #It will include the 0th term until custom(n-1)th term
print(names[:6]) #It will include the 0th term until custom(n-1)th term
print(names[:]) #It will include the 0th term until last term
# print(egname[-1: len(egname)]) #the -1 will simply take the len - 1th term
print(egname[-3:-1]) #the -1 will simply take the len - 1th term
name = "harry"
print(name[-4:-2])